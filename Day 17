#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

vector<long long> primeFactorization(long long N) {
    vector<long long> factors;

    // Step 1: handle factor 2
    while (N % 2 == 0) {
        factors.push_back(2);
        N /= 2;
    }

    // Step 2: handle odd factors
    for (long long i = 3; i * i <= N; i += 2) {
        while (N % i == 0) {
            factors.push_back(i);
            N /= i;
        }
    }

    // Step 3: if N is still > 1, it's prime
    if (N > 1) {
        factors.push_back(N);
    }

    return factors;
}

int main() {
    long long N;
    cin >> N;

    vector<long long> factors = primeFactorization(N);

    for (long long f : factors) {
        cout << f << " ";
    }
    cout << endl;

    return 0;
}
